<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"	
 	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reserv">
	<insert id="add" parameterType="java.util.HashMap">
		INSERT INTO ig_reserv VALUES ( r_num, r_guide, r_day, r_time, r_content, m_id)
		VALUES ( rum.NEXTVAL, #{r_guide},SYSDATE, #{r_time}, #{r_content}, 
		SELECT m_id FROM ig_member where m_id = #{id})
	</insert>
</mapper>
